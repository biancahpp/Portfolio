import React from 'react';
interface Props{
  image: string,
  title: string,
  tags: string[],
  description: string,
  demoUrl: string,
  codeUrl: string
}

export default function Projects({image, title, tags, description, demoUrl, codeUrl}:Props) {
  return (
    <div>
      
    </div>
  )
}
